// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update `site` to your production domain before deploying.
  site: 'https://apps-analyzer.example.com',
});
